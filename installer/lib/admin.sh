#!/usr/bin/env bash
# WooPrice Beta — Admin account creation
#
# Creates the initial admin account via the bootstrap-admin API endpoint.
# Prints credentials to terminal once and writes to install log (mode 600).
#
# NOT IMPLEMENTED IN B4.
# Implementation begins in B7 (Authentication Foundation).
# Requires: running app container (B6), JWT auth system (B7).

echo "lib/admin.sh — NOT IMPLEMENTED IN B4."
echo "Implementation begins in B7 (Authentication Foundation)."
exit 1
