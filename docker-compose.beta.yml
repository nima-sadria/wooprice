# WooPrice Beta — Docker Compose configuration
#
# PLACEHOLDER — this file is generated by the installer (B4).
# Do not edit manually. Use: wooprice configure
#
# The real docker-compose.beta.yml is generated from:
#   installer/templates/docker-compose.template.yml
# by installer/lib/compose_gen.sh during installation.
#
# See: docs/beta/DEPLOYMENT_ARCHITECTURE.md
# B4 — Installer Foundation: generates this file from template.
# B6 — Docker Runtime Foundation: implements full service definitions.
#
# Note: this file intentionally contains no real configuration.
# It serves as a marker so git tracks the file path.

version: "3.9"
# Services defined in B6 (Docker Runtime Foundation); file generated by installer in B4.
