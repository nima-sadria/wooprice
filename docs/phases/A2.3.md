# A2.3 — Transformation Rule Engine

## Phase

A2.3

## Name

Transformation Rule Engine

## Status

CLOSED

## Mission

Transform approved source data into deterministic, reproducible, and fully explainable Price Proposals. The Rule Engine is the single authoritative component responsible for converting raw source inputs into structured proposals. It does not apply prices, validate safety, compute change sets, or write to any destination.

---

## Scope

- Rule Engine architecture and interface
- Rule Definition models (types, parameters, priority, versioning)
- Rule Precedence system (order of rule evaluation)
- Rule Versioning (immutable rule versions; audit-ready history)
- Formula Engine — primary model: **Cost + Profit → Proposed Sell Price**
- Future-ready model: Competitor Price + Competition Rule → Proposed Sell Price (input support only)
- `PriceProposal` model (structured output of rule evaluation)
- Proposal provenance tracking (which rules, which inputs, which version produced this proposal)
- Rule repository (persistence of rule definitions and versions)
- Proposal repository (persistence of price proposals)
- Rule execution trace (per-proposal audit record of rule evaluation steps)
- Deterministic execution guarantee (identical inputs always produce identical proposals)
- Explainable proposal generation (every proposal fully derivable from stored provenance)
- A2 PostgreSQL migration for rule and proposal tables

## Out of Scope

- Apply Workflow
- Dry Run Engine
- Safety Policy Engine
- Change Set Engine
- WooCommerce write operations
- Execution Engine
- Scheduling Engine
- AI recommendations
- Competitor price scraping
- Competitor APIs
- External market data collection
- Any modification to existing Workspace, Preview Engine, or production Apply path

---

## Architecture Rules

- Rule Engine outputs Price Proposals only. A Price Proposal is **not** a Final Applied Price.
- Identical inputs must always produce identical Price Proposals (determinism guarantee).
- Rule execution must be deterministic; no random, time-dependent, or external-query behavior is permitted during rule evaluation.
- Proposal generation must be fully reproducible from stored provenance; given the stored rule version and stored source snapshot, any proposal must be re-derivable at any future time.
- Competitor price is an input only. The Rule Engine may accept competitor price as a data input when provided; it must never collect, scrape, or fetch competitor prices autonomously.
- Rule versions are immutable once published; a new rule version must be created for any change.
- No rule evaluation may write to WooCommerce or any external destination.

---

## Deliverables

- Rule Engine (core evaluation engine)
- Rule Definition models (type system, parameters, constraints)
- Rule Precedence system
- Rule Versioning (immutable versions with audit history)
- Formula Engine (Cost + Profit → Proposed Sell Price; extensible for future models)
- `PriceProposal` model
- Proposal provenance model
- Rule repository
- Proposal repository
- Rule execution trace / audit record
- Deterministic execution guarantee (enforced in tests)
- Explainable proposal generation
- A2 PostgreSQL migration for rule and proposal tables
- Unit and integration tests for all Rule Engine components

---

## Database / Migration Notes

- New A2 PostgreSQL migration required for: rule definition tables, rule version tables, price proposal tables, proposal provenance tables, rule execution trace tables.
- Migration must be additive; no existing A2 or SQLite tables may be modified.
- SQLite (`wooprice.db`) not modified.
- Default production stack not modified.

---

## Testing Requirements

- Unit tests for all rule types and formula evaluations
- Determinism tests: same inputs → same outputs across multiple runs and environments
- Reproducibility tests: proposals re-derivable from stored provenance
- Rule versioning tests: rule mutations create new versions; old versions remain valid
- Rule precedence tests: correct evaluation order for overlapping rules
- Proposal provenance tests: every proposal traceable to its rule version and source snapshot
- Integration tests against test PostgreSQL instance
- All existing application tests (`pytest`) must continue to pass unchanged

---

## Documentation Requirements

- `docs/A2_ARCHITECTURE.md` updated with Rule Engine description
- `docs/ROADMAP.md` A2.3 status updated upon completion
- `docs/PLATFORM_MAP.md` Section H updated upon completion
- Phase document (`docs/phases/A2.3.md`) kept current throughout implementation

---

## Phase Exit Criteria

- All deliverables complete and present in the codebase
- All A2.3 unit and integration tests passing
- Determinism guarantee verified by automated test
- Reproducibility guarantee verified by automated test
- All existing application tests passing
- Build passes (`npm run build` if frontend changes present)
- Independent Review (Step 3 of `.claude/WORKFLOW.md`) complete — no BLOCKER or HIGH findings
- Phase Completion Report delivered (all required fields including Build Status and Test Status)
- CHAT2 Architecture and Governance Review: APPROVE
- Owner approval obtained
- Stabilization commit created

---

## Production Safety Rules

**Global A2 Rules — apply to every A2 phase:**

- A2 must remain additive until reconciliation and parity verification pass.
- Every phase must remain independently auditable.
- Each phase must preserve backward compatibility with the existing Workspace until production cutover is explicitly approved.
- No phase may introduce functionality belonging to future phases.
- No production cutover without explicit Owner approval.
- No production deployment during A2 development unless explicitly approved by the Owner.
- No Apply integration outside its approved phase.
- No Dry Run integration outside its approved phase.
- No Safety Engine integration outside its approved phase.
- No Change Set logic outside its approved phase.
- No WooCommerce write logic outside its approved phase.
- Protected systems always require explicit Owner approval before modification.

**Protected Systems — require explicit Owner approval before modification:**

- Workspace UI
- Preview Engine
- Existing Cache Synchronization
- Apply Workflow
- Dry Run Workflow
- Emergency Apply
- JWT/Auth
- Pricing Validation
- WooCommerce Write Path
- Database Migrations
- Maintenance Mode
- Production Deployment
- Rollback / Undo

---

## Owner Decisions

- **Competitor price is an input only.** The Rule Engine may accept competitor price as a data input when provided by an approved adapter. The Rule Engine must never autonomously fetch, scrape, or collect competitor prices.
- **Forbidden in this phase and all future phases unless explicitly approved:** competitor price scraping, competitor API integration, external market data collection.
- Competitor Price model is future-ready but the collection mechanism is outside scope until Owner explicitly approves a source adapter for competitor data.

---

## Implementation Status

### Original A2.3 (LOCAL — superseded)

COMPLETE (commit `0e6f82b`). Independent Review complete. CHAT2 PASS. Owner approved 2026-06-25.
This implementation was superseded by the R2 reconciliation (see below).

### A2.3-R2 Reconciliation (current)

CLOSED — Architecture Verification: PASS. Owner approved 2026-06-26.

R2 was triggered by discovery of a divergent REMOTE implementation (commit `7b7b0eb`) with an
AST-based sandboxed formula engine that was architecturally superior to the LOCAL formula engine.
Owner decision (2026-06-25) selected Option 3: Reconcile — merge REMOTE engine design with LOCAL
audit/provenance model.

R2 key changes from original A2.3:
- Engine: AST-based sandboxed formula evaluator replaces original CostPlusProfitFormula
- 5 rule types supported: cost_plus, fx_based, fee_based, formula, competition
- Module structure: `app/a2/rules/` package (was `app/a2/engines/`)
- Migration: `a2_002_r2` replaces `a2_002` (non-conflicting revision)
- Hash determinism: proposal_hash excludes UUID and timestamp (was non-deterministic in REMOTE)
- Price precision: `Numeric(14,4)` column type (resolves TD-A2.3-001)
- ProposalEnvelope: propose() returns (PriceProposal, [TraceStep]) together
- Immutability: `is_published` guard (ValueError on re-publish) + `set_current_version()` for switching
- Test count: 129 A2.3-R2 tests + 475 existing = 604 total, 0 failures

R2 deliverables:
- `app/a2/rules/base.py` — RuleType enum, RuleDefinition frozen dataclass
- `app/a2/rules/formula.py` — AST-based evaluator (_ALLOWED_NODES whitelist, no eval/exec)
- `app/a2/rules/engine.py` — RuleEngine with ProposalEnvelope, TraceStep (3 steps per proposal)
- `app/a2/rules/proposal.py` — compute_proposal_hash (deterministic)
- `app/a2/models/pricing_rule.py` — PricingRule (table: a2_pricing_rules)
- `app/a2/models/pricing_rule_version.py` — PricingRuleVersion (is_published immutability)
- `app/a2/models/price_proposal.py` — PriceProposalRecord, ProposalProvenanceRecord, ExecutionTraceRecord
- `app/a2/repositories/rule_repository.py` — publish_version() immutability + load_active_definitions()
- `app/a2/repositories/proposal_repository.py` — save(envelope) + find_by_hash() deduplication
- `alembic_a2/versions/a2_002_r2_transformation_rule_engine.py` — 5 a2_-prefixed tables
- `tests/a2/test_a2_rule_formula.py` — 35 tests
- `tests/a2/test_a2_proposal.py` — 14 tests
- `tests/a2/test_a2_rule_engine.py` — 38 tests
- `tests/a2/test_a2_rule_repository.py` — 25 tests
- `tests/a2/test_a2_rules_isolation.py` — 14 tests

---

## Review Status

R2: CLOSED — Architecture Verification: PASS. CHAT2 conditional accept. Owner approved 2026-06-26.

Original A2.3: CLOSED — CHAT2 PASS, Owner approved 2026-06-25 (superseded by R2).

---

## Phase Completion Report Summary

### Original A2.3 (superseded)

**Test Status:** PASS — 485 total (62 A2.3 + 423 existing), 0 failures.

**Independent Review findings (original):**

| ID | Severity | Description | Resolution |
|---|---|---|---|
| A-1 | LOW | `Float` storage for proposal prices | RESOLVED in R2 — Numeric(14,4) |
| A-2 | LOW | `RuleDefinition.updated_at` write-once | CARRIED FORWARD as TD-A2.3-002 |
| T-1 | LOW | `test_rounding_ceil` uses exact integer input | N/A — rounding tests replaced by AST engine tests |
| T-2 | LOW | `test_evaluate_unknown_rule_type_raises` calls private `_dispatch` | N/A — engine rewritten |

### A2.3-R2 (CLOSED — Owner approved 2026-06-26)

**Test Status:** PASS — 604 total (129 A2.3-R2 + 475 existing), 0 failures.

**Build Status:** No frontend changes. `npm run build` not applicable.

**Independent Review:** COMPLETE — BLOCKER: 0, HIGH: 0, MEDIUM: 0, LOW: 7 (4 resolved, 3 as TD).

**Architecture Verification:** PASS — canonical migration: `a2_002_r2`, 12/12 checks passed.

**CHAT2 Review:** Conditional accept — Architecture Verification performed and passed.

**Owner Approval:** 2026-06-26.

---

## Technical Debt

| ID | Description | Status |
|---|---|---|
| TD-A2.3-001 | Float storage for proposal prices | RESOLVED — Numeric(14,4) in a2_002_r2 migration |
| TD-A2.3-002 | `RuleDefinition` (PricingRule) updated_at is write-once; no update path in A2.3 | OPEN |

---

## Cross-References

- Architecture reference: [docs/A2_ARCHITECTURE.md](../A2_ARCHITECTURE.md)
- Phase roadmap: [docs/ROADMAP.md](../ROADMAP.md) — A2 Track section
- Platform architecture: [docs/PLATFORM_MAP.md](../PLATFORM_MAP.md) — Section H
- Workflow governance: [.claude/WORKFLOW.md](../../.claude/WORKFLOW.md)
- Governance rules: [.claude/GOVERNANCE.md](../../.claude/GOVERNANCE.md)
- Previous phase: [docs/phases/A2.2.md](A2.2.md)
- Next phase: [docs/phases/A2.4.md](A2.4.md)
