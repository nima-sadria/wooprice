# A2.9 — AI Foundation

## Phase

A2.9

## Name

AI Foundation

## Status

CLOSED

## Mission

Provide advisory intelligence that assists pricing decisions while remaining completely
outside the Trusted Execution Path. The AI Foundation generates signals, warnings,
and recommendations for human review only. It has no authority to apply prices,
override safety policies, bypass seller confirmation, or trigger any execution.

**A2.9 scope constraint**: No real AI API connections (no OpenAI, no Anthropic).
No existing Workspace, Apply, Dry Run, or Safety Engine logic modified. Advisory
output only — the only output type is AdvisoryInsight.

---

## Trusted Execution Path

The following pipeline is immutable. A2.9 must not modify, replace, intercept,
or bypass any component:

```
Source → Transformation Rule Engine → Safety Policy Engine → Change Set Engine
       → Dry Run Engine → Seller Confirmation → Execution Engine → Scheduling Engine
```

AI is advisory only. It sits alongside this path but never participates in it.

---

## Scope

- AdvisoryInsight model (category, severity, confidence, summary, explanation,
  evidence, related_object references, recommendation_trace, generated_at)
- AdvisorySession model (audit record: category, subject_type, subject_id,
  prompt_text, response_text, model_version, is_archived, created_at, closed_at)
- AdvisoryRepository (create_session, store_insight, list_insights, get_session,
  archive_session)
- AdvisoryService (generate_explanation, generate_risk_summary, detect_anomaly,
  detect_stale_price, assign_review_priority, generate_rule_recommendation)
- Insight generation interface (rule-based foundation; no external AI API in this phase)
- Insight categorization (EXPLANATION, RISK_SUMMARY, ANOMALY, STALE_PRICE,
  REVIEW_PRIORITY, RULE_RECOMMENDATION)
- Risk summarization
- Proposal explanation
- Anomaly explanation (price spike detection, zero/negative price detection)
- Stale price detection (source_age_hours thresholds)
- Review priority scoring (score-based: safety_result + price_change_pct + anomaly flag)
- Recommendation trace (recommendation_trace field on every insight)
- Recommendation confidence (confidence float on every insight)
- Prompt/response audit records (AdvisorySession.prompt_text, response_text)
- Alembic migration a2_008 (2 new tables: a2_advisory_sessions, a2_advisory_insights)
- 51 tests, all passing

## Out of Scope (A2.9)

- Real AI API connections (no OpenAI, no Anthropic APIs in this phase)
- Autonomous price application (no auto-apply)
- Safety policy override (no AI-driven safety policy bypass)
- Seller confirmation bypass (no AI-triggered confirmation)
- WooCommerce write operations (no destination writes by AI components)
- Autonomous execution, scheduling, or triggering
- Rule creation, modification, or publication (requires Owner-approval workflow via A2.3)
- Autonomous business-policy or safety-policy modification
- REST endpoints
- UI
- Any modification to existing Workspace, Apply, Dry Run, or Safety Engine logic

---

## Architecture Rules

- A2.9 is advisory only. All output is AdvisoryInsight. No executable domain objects
  (PriceProposal, ChangeSet, DryRunResult, ExecutionPlan, Schedule, ApplyCommand)
  are ever produced.
- Isolation is one-way: AI may read prior-phase outputs. Prior phases (Rule Engine,
  Safety Engine, Change Set Engine, Dry Run Engine, Execution Engine, Scheduling Engine)
  must never import from `app.a2.ai`.
- subject_id and related_object_id in advisory models are plain strings — no FK to
  prior-phase tables (phase independence).
- All insights are immutable once created. No update_insight operation exists.
- All sessions must be archived before insights in the session can be considered final.
- Every AdvisoryInsight includes: id, session_id, category, severity, confidence,
  summary, explanation, evidence, related_object_type, related_object_id,
  recommendation_trace, generated_at.
- No AI output may suppress or override a Safety Policy BLOCK result.
- No AI output may act as or replace Seller Confirmation.

---

## Deliverables

- `app/a2/ai/__init__.py` — AI Foundation package (isolation boundary declared)
- `app/a2/ai/models.py` — AdvisorySession, AdvisoryInsight ORM models
- `app/a2/ai/repository.py` — AdvisoryRepository: all required operations
- `app/a2/ai/service.py` — AdvisoryService: all advisory generation methods
- `alembic_a2/versions/a2_008_ai_foundation.py` — migration a2_008 (down_revision=a2_007)
- `alembic_a2/env.py` — updated to import app.a2.ai.models
- `tests/a2/test_a2_advisory.py` — 51 tests, all pass

---

## Database / Migration Notes

- Migration a2_008 is strictly additive: 2 new tables, no existing A2 or SQLite tables
  modified.
- SQLite (`wooprice.db`) not modified.
- Default production stack not modified.
- down_revision = a2_007

**Tables:**

| Table | Purpose |
|---|---|
| `a2_advisory_sessions` | Audit record for one advisory interaction |
| `a2_advisory_insights` | Single advisory finding; immutable once stored |

---

## Testing Requirements (all met)

| Category | Tests | Result |
|---|---|---|
| AdvisoryInsight creation | 3 | PASS |
| Explanation generation | 4 | PASS |
| Anomaly detection | 4 | PASS |
| Stale price detection | 3 | PASS |
| Review priority generation | 4 | PASS |
| Recommendation traceability | 4 | PASS |
| Confidence persistence | 2 | PASS |
| Immutable insights | 3 | PASS |
| Risk summary | 3 | PASS |
| Rule recommendation | 3 | PASS |
| Session lifecycle | 3 | PASS |
| Migration | 4 | PASS |
| Isolation | 11 | PASS |
| **Total A2.9** | **51** | **PASS** |
| **Full Suite** | **911** | **PASS** |

---

## Implementation Status

CLOSED. All deliverables delivered, verified, and approved by Owner 2026-06-26.

Key commits: 4a4777d

Key files added:
- `app/a2/ai/__init__.py`
- `app/a2/ai/models.py`
- `app/a2/ai/repository.py`
- `app/a2/ai/service.py`
- `alembic_a2/versions/a2_008_ai_foundation.py`
- `tests/a2/test_a2_advisory.py`

Modified:
- `alembic_a2/env.py` — imports app.a2.ai.models
- `docs/phases/A2.9.md` — this document
- `docs/A2_ARCHITECTURE.md` — A2.9 description; status READY FOR OWNER REVIEW
- `docs/ROADMAP.md` — A2.9 status READY FOR OWNER REVIEW
- `docs/PLATFORM_MAP.md` — A2.9 status READY FOR OWNER REVIEW

---

## Review Status

CLOSED — Owner approved 2026-06-26.

Architecture Verification: PASS (CHAT2, commit 4a4777d).
Independent Review: PASS (0 BLOCKER, 0 HIGH, 0 MEDIUM, 0 LOW findings).
Tests: 911 passed, 0 failed.
Migration: a2_008 (down_revision=a2_007).
AI Isolation: PASS — no TEP module imports from app.a2.ai.
Trusted Execution Path: unchanged by A2.9.
Output Contract: AdvisoryInsight only — no executable domain objects produced.
Beta Strategy: documented in docs/BETA_STRATEGY.md (commit 5e6c05f).

Technical Debt (permanent record):
- TD-A2.9-001: OPEN — AdvisoryService uses rule-based logic only; no real AI API
  connected in this phase. Advisory quality improves when a real LLM backend is wired in
  (future phase).
- TD-A2.8-001: OPEN (inherited)
- TD-A2.7-002: OPEN (inherited)
- TD-A2.7-003: OPEN (inherited)
- TD-A2.6-001: OPEN (inherited)
- TD-A2.6-002: OPEN (inherited)
- TD-A2.5-002: OPEN (inherited)
- TD-A2.5-003: OPEN (inherited)
- TD-A2.5-004: OPEN (inherited)

---

## A2 Platform Core

COMPLETE.

All phases delivered, verified, reviewed, and Owner-approved:

- A2.1 — Canonical Product Model + PostgreSQL Foundation: CLOSED
- A2.2 — Source Adapter Framework: CLOSED
- A2.3-R2 — Transformation Rule Engine: CLOSED
- A2.4 — Safety Policy Engine: CLOSED
- A2.5 — Change Set Engine: CLOSED
- A2.6 — Dry Run Engine: CLOSED
- A2.7 — Execution Engine: CLOSED
- A2.8 — Scheduling Engine: CLOSED
- A2.9 — AI Foundation: CLOSED

No production cutover has occurred.
No production deployment has occurred.
Production WooPrice remains maintenance-only.
Future product and platform development moves to WooPrice Beta (docs/BETA_STRATEGY.md).

---

## Phase Exit Criteria

- [x] All deliverables complete and present
- [x] All A2.9 tests passing (51 tests)
- [x] Full suite passing (911 tests, 0 failures)
- [x] AI isolation from Trusted Execution Path verified by tests
- [x] Prior phases verified to not import from app.a2.ai
- [x] No AI output triggers execution (no dispatch/execute/schedule in AI modules)
- [x] No AI output suppresses Safety Policy BLOCK (no safety engine modification)
- [x] No external AI API calls (no OpenAI/Anthropic in A2.9)
- [x] Recommendation trace stored on every insight (verified by tests)
- [x] Confidence persistence verified by tests
- [x] Immutability: no update_insight operation exists
- [x] Migration additive: only 2 new tables, no existing tables modified
- [x] Independent Review complete — 0 BLOCKER, 0 HIGH, 0 MEDIUM findings
- [x] Phase Completion Report delivered
- [x] CHAT2 Architecture and Governance Review: PASS
- [x] Owner approval: 2026-06-26
- [x] Stabilization commit: 4a4777d

---

## Production Safety Rules

**Global A2 Rules — apply to every A2 phase:**

- A2 must remain additive until reconciliation and parity verification pass.
- Every phase must remain independently auditable.
- Each phase must preserve backward compatibility with the existing Workspace until
  production cutover is explicitly approved.
- No phase may introduce functionality belonging to future phases.
- No production cutover without explicit Owner approval.
- No production deployment during A2 development unless explicitly approved by the Owner.
- No Apply integration outside its approved phase.
- No Dry Run integration outside its approved phase.
- No Safety Engine integration outside its approved phase.
- No Change Set logic outside its approved phase.
- No WooCommerce write logic outside its approved phase.
- Protected systems always require explicit Owner approval before modification.

**Protected Systems — require explicit Owner approval before modification:**

- Workspace UI
- Preview Engine
- Existing Cache Synchronization
- Apply Workflow
- Dry Run Workflow
- Emergency Apply
- JWT/Auth
- Pricing Validation
- WooCommerce Write Path
- Database Migrations
- Maintenance Mode
- Production Deployment
- Rollback / Undo

---

## Owner Decisions

- **No auto apply.** AI components may never trigger execution.
- **No safety override.** AI components may never bypass or suppress a Safety Policy result.
- **No seller confirmation bypass.** AI components may never act as a seller confirmation.
- **No WooCommerce write.** AI components hold no destination credentials and make no
  destination writes.
- **No autonomous execution.** AI components may not initiate, schedule, or trigger any
  Change Set execution.
- **No rule generation without Owner approval.** AI may not create or publish pricing
  rules without explicit Owner authorization for each specific rule.
- **No autonomous business-policy modification.** AI may not modify business rules.
- **No autonomous safety-policy modification.** AI may not modify safety thresholds or
  policy modes.
- **No real AI API in this phase.** No OpenAI, Anthropic, or other external AI API
  connections until explicitly approved by Owner.

---

## Cross-References

- Architecture reference: [docs/A2_ARCHITECTURE.md](../A2_ARCHITECTURE.md)
- Phase roadmap: [docs/ROADMAP.md](../ROADMAP.md) — A2 Track section
- Platform architecture: [docs/PLATFORM_MAP.md](../PLATFORM_MAP.md) — Section H
- Workflow governance: [.claude/WORKFLOW.md](../../.claude/WORKFLOW.md)
- Governance rules: [.claude/GOVERNANCE.md](../../.claude/GOVERNANCE.md)
- Previous phase: [docs/phases/A2.8.md](A2.8.md)
- Beta strategy: [docs/BETA_STRATEGY.md](../BETA_STRATEGY.md) — all future new work targets Beta
